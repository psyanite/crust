import 'package:flutter/material.dart';

class RewardsScreen extends StatelessWidget {
    RewardsScreen({Key key}) : super(key: key);

    @override
    Widget build(BuildContext context) {
        return new Center(
            child: new Text('Rewards'),
        );
    }

}